Drop an official First Hospitality logo asset here only if one has been
legitimately supplied. Until then the header uses a typographic lockup
("First Hospitality x Archer Design") -- do not scrape or approximate the
company's logo.
