// ─────────────────────────────────────────────────────────────────────────────
// dns-media.ts — media asset references for the /dns private partnership
// concept. Every path below already exists in this project's shared media
// library at public/tcrm/ (previously used on /tcrm and, in the case of
// hotel-arrival-style clips, other private proposal pages). Nothing here is
// duplicated or newly uploaded — these are existing, optimized project
// assets reused by reference.
//
// None of this imagery is DNS Industries work. It is Archer Design's own
// hospitality creative library, used here as neutral, high-end hospitality
// context — never captioned or implied as a DNS-built project. The large
// motion carousel (section 02) and static gallery (section 03) pull their
// own media manifests directly from app/tcrm/tcrm-motion-blob-manifest.ts
// and app/tcrm/tcrm-media.ts respectively (see DnsMotionShowcase and
// DnsStillsGallery) rather than duplicating those lists here.
// ─────────────────────────────────────────────────────────────────────────────

/** Hero: full-bleed background video, a hospitality environment shown as
 * neutral context for the "physical space → guest-facing activation" idea —
 * not DNS work, never captioned as such. Reuses the same clip and poster
 * already used as /tcrm's own hero background (existing, optimized project
 * assets, reused by reference — nothing duplicated or newly uploaded). */
export const HERO_ENVIRONMENT_VIDEO = "/tcrm/videos/luxury-hotel-entrance-night-concept.mp4";
export const HERO_ENVIRONMENT_POSTER = "/tcrm/images/tcrm-hero-poster.webp";
export const HERO_ENVIRONMENT_ALT = "Hospitality environment at night, shown as neutral context — not DNS Industries work";
